# 校园通小程序 AI 助手优化 & 架构重构

## 完成内容

### 1. AI 助手"卡住了" — 根因修复
- **Bug 修复**：`requestAdapter.js` 中 mock 模式下助手网关跳过 mode 检查，直接发 HTTP 请求 → 必然失败卡死
- **策略控制**：新增 `aiService.js` 统一策略层，`mode !== 'http'` 时走纯本地知识库，零延迟响应

### 2. 架构重构 — 三层解耦
```
assistant.js (纯 UI)
    ↓
aiService.js (策略编排 + 回退)
    ↓
aiEngine.js (10条知识库) | wx.request (云端 API)
```
- 请求链路从 3 层嵌套回退简化到 1 层统一入口
- 新增健康检查 + 15s 超时 + 请求取消机制

### 3. 用户体验优化
- 连接状态实时显示
- 消息持久化（避免退出丢记录）
- 取消按钮（云端请求时可用）
- 脉动动画替代静态"思考中"

### 修改文件：7 个 | 新增文件：2 个 | 测试状态：通过（无新失败）
